# Copy to .env and fill in.
PG_PASSWORD=

# Optional: enable Umami pageview analytics. Both required to render the
# tracking script; either missing → skipped silently.
NEXT_PUBLIC_UMAMI_SRC=
NEXT_PUBLIC_UMAMI_WEBSITE_ID=
